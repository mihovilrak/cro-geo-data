from django.apps import AppConfig

class CadastralConfig(AppConfig):
    """
    Django app configuration for the cadastral application.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'cadastral'
